import org.scalatest.FunSuite
import org.scalatest.matchers.ShouldMatchers._

class Homework3 extends FunSuite {

  /*
  test("can create a class") {
    val coolCat = new CatPlayingPiano("bob")
    coolCat.name should be ("bob")
    coolCat.isHowlingAtTheMoon should be (true)
  }

  test("no two cats are the same") {
    val myVic = new CatPlayingPiano("vic")
    val yourVic = new CatPlayingPiano("vic")
    myVic should not be === (yourVic)
  }

  test("a number is a number even if it's complex") {
    val pureImagination = Complex(0, 1)
    val i = Complex(0, 1)

    pureImagination should be === i
  }
  */
}
